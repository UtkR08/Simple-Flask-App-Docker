apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  labels:
    app: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-appa
    spec:
      containers:
        - name: myapp
          image: demolearnacr.azurecr.io/demo_dev/product-list-app:5e09ecc545fb6a9cc618bfd28e08d4dbcdf21f79
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5070
      imagePullSecrets:
        - name: my-secret
